package components

import (
	"os"
	"log"
	"strings"
)

func getImages() []string {
	images := []string{}

	c, err := os.ReadDir("assets/images/examples")
	if err != nil {
		log.Fatal(err)
	}

	for _, entry := range c {
		if strings.Contains(entry.Name(), "cropped") {
			images = append(images, entry.Name())
		}
	}

	return images
}

templ examples() {
	<div class="p-4 space-x-4 carousel carousel-center">
		for _, image := range getImages() {
			<div class="md:translate-x-full carousel-item md:animate-scroll md:timeline-scroll">
				<img
					src={ "assets/images/examples/" + image }
					class="w-80 h-auto sm:w-96 sm:h-64 rounded-box"
					loading="lazy"
					alt="Пример упражнений"
				/>
			</div>
		}
	</div>
}
